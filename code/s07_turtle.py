import turtle

t = turtle.Turtle()
t.speed(0)

t.forward(100)
t.left(90)



# turtle.done() # makes sure the tutle graphics window stays open until you close it
turtle.mainloop() # this is the same as turtle.done() but it is more corss-platform compatible